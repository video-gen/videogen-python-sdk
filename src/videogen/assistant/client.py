# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.assistant_turn_response import AssistantTurnResponse
from .raw_client import AsyncRawAssistantClient, RawAssistantClient
from .types.act_on_assistant_action_request_decision import ActOnAssistantActionRequestDecision

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AssistantClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAssistantClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAssistantClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAssistantClient
        """
        return self._raw_client

    def start_assistant_chat(
        self,
        *,
        message: str,
        force_workflow_suggestion: typing.Optional[bool] = OMIT,
        auto_generate: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AssistantTurnResponse:
        """
        Creates a new project and sends the first message to the VideoGen AI assistant, exactly like typing into the assistant on a new project in the app. Synchronous: the response includes the assistant's reply, any workflow suggestions it offered, and a `projectUrl` to open the project in the app. Continue the conversation with `POST /v1/assistant/chats/{projectId}/messages`.

        Parameters
        ----------
        message : str
            The first message to send to the assistant, exactly as a person would type it into the assistant on a new project.

        force_workflow_suggestion : typing.Optional[bool]
            When true, the assistant always responds with at least one workflow suggestion (the fastest way to get a starting point). When false, the assistant decides whether to suggest a workflow or just reply.

        auto_generate : typing.Optional[bool]
            When true, the assistant picks the best workflow for the message and immediately starts generating, skipping the suggestion step. The response's `status` is `generating` and `generation` carries the run to poll. When false, nothing generates until you act on a suggestion or plan.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AssistantTurnResponse
            The assistant's reply.

        Examples
        --------
        from videogen import VideoGenApi

        client = VideoGenApi(
            token="YOUR_TOKEN",
        )
        client.assistant.start_assistant_chat(
            message="A 30-second explainer about our new pricing tiers",
        )
        """
        _response = self._raw_client.start_assistant_chat(
            message=message,
            force_workflow_suggestion=force_workflow_suggestion,
            auto_generate=auto_generate,
            request_options=request_options,
        )
        return _response.data

    def send_assistant_message(
        self, project_id: str, *, message: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AssistantTurnResponse:
        """
        Sends a follow-up message to the assistant in an existing project chat started with `POST /v1/assistant/chats`. Synchronous: the response includes the assistant's reply and any actions it offered.

        Parameters
        ----------
        project_id : str
            The project id (e.g. `vg_proj_...`).

        message : str
            The message to send to the assistant in this project chat.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AssistantTurnResponse
            The assistant's reply.

        Examples
        --------
        from videogen import VideoGenApi

        client = VideoGenApi(
            token="YOUR_TOKEN",
        )
        client.assistant.send_assistant_message(
            project_id="projectId",
            message="Make it more upbeat and add captions",
        )
        """
        _response = self._raw_client.send_assistant_message(
            project_id, message=message, request_options=request_options
        )
        return _response.data

    def act_on_assistant_action(
        self,
        project_id: str,
        assistant_action_id: str,
        *,
        decision: typing.Optional[ActOnAssistantActionRequestDecision] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AssistantTurnResponse:
        """
        Acts on an actionable widget the assistant offered on a prior turn. Reference the action by its `assistantActionId` from a prior `AssistantTurnResponse`. Today this accepts a workflow suggestion: `APPROVE` (the default) applies the suggested workflow and immediately starts generating — the response's `status` is `generating` and `generation` carries the run to poll — while `REJECT` returns the offering turn unchanged. Every other action has `requiresApp` set to true and can only be completed in the web app; open `projectUrl` instead.

        Parameters
        ----------
        project_id : str
            The project id (e.g. `vg_proj_...`).

        assistant_action_id : str
            The `assistantActionId` of the action to act on, from a prior `AssistantTurnResponse`.

        decision : typing.Optional[ActOnAssistantActionRequestDecision]
            Whether to approve (apply) or reject the action. Defaults to `APPROVE`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AssistantTurnResponse
            The assistant's reply after acting on the action.

        Examples
        --------
        from videogen import VideoGenApi

        client = VideoGenApi(
            token="YOUR_TOKEN",
        )
        client.assistant.act_on_assistant_action(
            project_id="projectId",
            assistant_action_id="assistantActionId",
        )
        """
        _response = self._raw_client.act_on_assistant_action(
            project_id, assistant_action_id, decision=decision, request_options=request_options
        )
        return _response.data


class AsyncAssistantClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAssistantClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAssistantClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAssistantClient
        """
        return self._raw_client

    async def start_assistant_chat(
        self,
        *,
        message: str,
        force_workflow_suggestion: typing.Optional[bool] = OMIT,
        auto_generate: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AssistantTurnResponse:
        """
        Creates a new project and sends the first message to the VideoGen AI assistant, exactly like typing into the assistant on a new project in the app. Synchronous: the response includes the assistant's reply, any workflow suggestions it offered, and a `projectUrl` to open the project in the app. Continue the conversation with `POST /v1/assistant/chats/{projectId}/messages`.

        Parameters
        ----------
        message : str
            The first message to send to the assistant, exactly as a person would type it into the assistant on a new project.

        force_workflow_suggestion : typing.Optional[bool]
            When true, the assistant always responds with at least one workflow suggestion (the fastest way to get a starting point). When false, the assistant decides whether to suggest a workflow or just reply.

        auto_generate : typing.Optional[bool]
            When true, the assistant picks the best workflow for the message and immediately starts generating, skipping the suggestion step. The response's `status` is `generating` and `generation` carries the run to poll. When false, nothing generates until you act on a suggestion or plan.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AssistantTurnResponse
            The assistant's reply.

        Examples
        --------
        import asyncio

        from videogen import AsyncVideoGenApi

        client = AsyncVideoGenApi(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.assistant.start_assistant_chat(
                message="A 30-second explainer about our new pricing tiers",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.start_assistant_chat(
            message=message,
            force_workflow_suggestion=force_workflow_suggestion,
            auto_generate=auto_generate,
            request_options=request_options,
        )
        return _response.data

    async def send_assistant_message(
        self, project_id: str, *, message: str, request_options: typing.Optional[RequestOptions] = None
    ) -> AssistantTurnResponse:
        """
        Sends a follow-up message to the assistant in an existing project chat started with `POST /v1/assistant/chats`. Synchronous: the response includes the assistant's reply and any actions it offered.

        Parameters
        ----------
        project_id : str
            The project id (e.g. `vg_proj_...`).

        message : str
            The message to send to the assistant in this project chat.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AssistantTurnResponse
            The assistant's reply.

        Examples
        --------
        import asyncio

        from videogen import AsyncVideoGenApi

        client = AsyncVideoGenApi(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.assistant.send_assistant_message(
                project_id="projectId",
                message="Make it more upbeat and add captions",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.send_assistant_message(
            project_id, message=message, request_options=request_options
        )
        return _response.data

    async def act_on_assistant_action(
        self,
        project_id: str,
        assistant_action_id: str,
        *,
        decision: typing.Optional[ActOnAssistantActionRequestDecision] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AssistantTurnResponse:
        """
        Acts on an actionable widget the assistant offered on a prior turn. Reference the action by its `assistantActionId` from a prior `AssistantTurnResponse`. Today this accepts a workflow suggestion: `APPROVE` (the default) applies the suggested workflow and immediately starts generating — the response's `status` is `generating` and `generation` carries the run to poll — while `REJECT` returns the offering turn unchanged. Every other action has `requiresApp` set to true and can only be completed in the web app; open `projectUrl` instead.

        Parameters
        ----------
        project_id : str
            The project id (e.g. `vg_proj_...`).

        assistant_action_id : str
            The `assistantActionId` of the action to act on, from a prior `AssistantTurnResponse`.

        decision : typing.Optional[ActOnAssistantActionRequestDecision]
            Whether to approve (apply) or reject the action. Defaults to `APPROVE`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AssistantTurnResponse
            The assistant's reply after acting on the action.

        Examples
        --------
        import asyncio

        from videogen import AsyncVideoGenApi

        client = AsyncVideoGenApi(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.assistant.act_on_assistant_action(
                project_id="projectId",
                assistant_action_id="assistantActionId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.act_on_assistant_action(
            project_id, assistant_action_id, decision=decision, request_options=request_options
        )
        return _response.data
