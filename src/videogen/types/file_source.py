# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .file_source_status import FileSourceStatus


class FileSource(UniversalBaseModel):
    """
    A rendition source for a file (e.g. thumbnail, preview, download). Contains a signed URL and metadata.
    """

    status: FileSourceStatus = pydantic.Field()
    """
    `pending` — asset is still processing or has not been hydrated yet. `ready` — signed URL is available. `failed` — rendition generation failed. `skipped` — rendition does not apply to this file type (e.g. thumbnail for audio).
    """

    url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Signed URL. Present when status is `ready` and file has been recently hydrated. If missing, call the hydrate endpoint.
    """

    expires_at: typing_extensions.Annotated[
        typing.Optional[float],
        FieldMetadata(alias="expiresAt"),
        pydantic.Field(
            alias="expiresAt", description="Seconds since epoch (Unix timestamp) when the signed URL expires."
        ),
    ] = None
    width: typing.Optional[int] = pydantic.Field(default=None)
    """
    Rendition width in pixels, when known.
    """

    height: typing.Optional[int] = pydantic.Field(default=None)
    """
    Rendition height in pixels, when known.
    """

    file_bytes: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="fileBytes"),
        pydantic.Field(alias="fileBytes", description="File size in bytes, when known."),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
