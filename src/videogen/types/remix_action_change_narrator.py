# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class RemixActionChangeNarrator(UniversalBaseModel):
    """
    Re-narrate every AI-voiceover asset in the project with a new voice (and optionally a presenter avatar), replacing each narration in place. Runs asynchronously: text-to-speech is re-fired per asset with the original narration text. If the project has no AI-narrated assets, the action is skipped and completes successfully without changing anything.
    """

    voice_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="voiceId"),
        pydantic.Field(
            alias="voiceId",
            description="Voice id from `GET /v1/resources/tts-voices` (e.g. `vg_voic_...`) to re-narrate with.",
        ),
    ]
    avatar_presenter_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="avatarPresenterId"),
        pydantic.Field(
            alias="avatarPresenterId",
            description="Optional avatar presenter id from `GET /v1/resources/avatar-presenters` (e.g. `vg_pres_...`). When set, the narration is delivered by a talking-head presenter avatar. Pass your `voiceId` to that endpoint to list presenters sorted by best match for the voice. Omit or pass `null` for a standard voiceover with no presenter.",
        ),
    ] = None
    voice_speed: typing_extensions.Annotated[
        typing.Optional[float],
        FieldMetadata(alias="voiceSpeed"),
        pydantic.Field(
            alias="voiceSpeed",
            description="Speech rate multiplier. Omit or pass `null` to keep each asset's current speed.",
        ),
    ] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
