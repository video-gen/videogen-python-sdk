# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class EntityReference(UniversalBaseModel):
    """
    A file attached to an entity as a reference image.
    """

    file_id: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="fileId"),
        pydantic.Field(
            alias="fileId",
            description="The reference image file id (e.g. `vg_file_...`). Hydrate it via `GET /v1/files/{fileId}` to fetch a viewable URL.",
        ),
    ]
    description: str = pydantic.Field()
    """
    Optional description of the reference. Empty string when not set.
    """

    is_default: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="isDefault"),
        pydantic.Field(
            alias="isDefault", description="When true, this is the entity's primary reference (used for its thumbnail)."
        ),
    ]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
